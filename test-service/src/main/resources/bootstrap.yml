eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    healthcheck:
      enabled: true
spring:
  application:
    name: test-service
  cloud:
    config:
      uri: http://localhost:8888
server:
  port: 8090